# MEMORY.md

- Preferencia estable del usuario: mantener el heartbeat deshabilitado para evitar costos excesivos. Si se vuelve a necesitar automatización, preferir cron o activarlo solo con indicación explícita.
- Al revisar workspaces relacionados, confirmar que `workspace-admin` y `workspace-empleado` mantengan heartbeat deshabilitado o `HEARTBEAT.md` sin tareas activas.
