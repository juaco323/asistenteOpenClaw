- Telegram «en bucle» / no atiende contraseña o comandos en /chat: la contraseña solo la leía `routed_text_message` fuera del ConversationHandler; dentro de /chat iba a OpenClaw. Mensajes autenticados sin /chat recibían menú repetido. Fix: contraseña en `forward_chat_message`, consultas autenticadas van directo al gateway, `/workspace` y `/estado` dentro del modo chat.
- Telegram lento / sin respuesta en «dame lista top 5…»: `parse_file_request` interpretaba «dame X» como pedido de archivo (patrón demasiado amplio). Fix: solo archivo explícito o extensión; indicador «Procesando…» + typing; reglas Telegram en AGENTS/context.
- Correo Valorant enviado con cuerpo mal formateado (`$`, `\n\n` literales): causa = agente usó `--body "..."` con escapes JSON/shell. Fix documentación + **parche en `gog-wrapper.sh`**: `gog-gmail-draft-wrap.py` convierte `\n`→saltos reales y quita `$` inicial en todo `gog gmail drafts create` (imágenes admin/empleado reconstruidas). `drafts send` no corrige borradores viejos: hace falta **borrador nuevo**.
