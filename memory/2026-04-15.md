- El usuario pidió deshabilitar heartbeat para evitar costos excesivos.
- Se configuró `agents.defaults.heartbeat.every` en `0m` en `~/.openclaw/openclaw.json`.
- Se verificó que no hay cron jobs configurados actualmente, por lo que deshabilitar heartbeat no afecta tareas programadas existentes.
- Se dejó `HEARTBEAT.md` sin tareas activas en el workspace principal, `workspace-admin` y `workspace-empleado`.
- Preferencia a recordar: mantener heartbeat deshabilitado en admin y trabajador, y usar cron solo cuando el usuario lo pida explícitamente.
