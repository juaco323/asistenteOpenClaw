- Usuario pidió arrancar todo para interactuar. Recreados `docker/{admin,empleado,telegram}/.env` desde `.env.example` + valores del último `docker inspect` (tokens, GOG, OpenAI). Admin en puerto **18791** (18789 ocupado por `node` local). Ejecutado `./docker/stack-up.sh` con entorno limpio; stacks admin, empleado y Telegram levantados.
- Usuario pidió reclonar desde https://github.com/juaco323/asistenteOpenClaw.git. Copia local ya estaba en `b1c1578` (= origin/main). Clon limpio: copia anterior → `asistenteOpenClaw.old-20260524`, backup `.env` en `.asistenteOpenClaw-backup-20260524-210144`, restaurados `.env` y stack relanzado.
- Revisión Telegram/correo: `.env` de Telegram en disco tenía `replace_me` (cambios solo en editor); persistido token + allowlist, bot `Application started`. Gmail OK en contenedores admin/empleado (`gog auth list`, labels); host `gog` con error de llavero (no bloquea Docker).
- Fallo envío Gmail en UI («falta GOG_KEYRING_PASSWORD»): OpenClaw no pasa la var al `exec`; reforzado entrypoint/gog-wrapper (copia clave a `/home/node/.openclaw/gog-keyring.pw` además de `/tmp/…`); imágenes admin/empleado reconstruidas y contenedores recreados.
- Telegram «No encontré el archivo» con archivos existentes en Descargas: `docker/telegram/docker-compose.yml` montaba `$TELEGRAM_HOST_HOME` en `/home/joaquin` fijo; bot buscaba `/home/fernanda2114t/...`. Corregido montaje `${HOST}:${HOST}`; contenedor recreado; `lista.txt` y pptx visibles dentro del bot.
