- Implementado registro de pruebas LLM admin: `docker/admin/llm-test-logger` (`oc.Tracker` local JSONL), panel `openclaw-admin-llm-panel`, comando Telegram `/prueba_llm` con volumen al mismo JSONL.
- Gmail / GOG: skill `skills/email-gmail`, protocolo en `workspace-empleado/AGENTS.md` y `workspace-admin/AGENTS.md`, `workspace-empleado/LOGS_EMAIL.md` + `HISTORY.md`, Docker empleado/admin con `GOG_*`, montaje `~/.config/gog`, `gog` instalado en imágenes; admin construye desde `docker/admin/Dockerfile`.
- Arranque stack: creados `docker/{admin,empleado,telegram}/.env` desde ejemplo con `OPENCLAW_HOST_HOME=/home/fernanda2114t`, tokens generados. Puerto **18789** ocupado por `node` local; gateway **admin** expuesto en **18791** (`docker/admin/.env`); `docker/telegram/.env` apunta `OPENCLAW_ADMIN_BASE_URL` a ese puerto. Empleado en **18790**; imagen empleado reconstruida con `gog`.
- Admin puede enviar Gmail con el mismo protocolo que empleado: `docker/admin/docker-compose` monta `~/.config/gog` y `workspace-empleado` (`/app/logs_shared`) en **rw**; `workspace-admin/AGENTS.md` y skill `email-gmail` actualizados.
- Remitente Gmail fijado a **prueba.openclaw.fj@gmail.com** (no usar otra cuenta por `gog auth list`); JSON OAuth **`~/Descargas/prueba_openclaw.fj.json`** (copia creada desde `prueba_openclaw_fj.json` si hacía falta).
- Docker: montaje corregido a **`~/.config/gogcli`** (no `.config/gog`); `chown` de `/home/node/.config` en las imágenes. Hasta completar **`gog auth add prueba.openclaw.fj@gmail.com`** en el host con el mismo `GOG_KEYRING_PASSWORD` que `.env`, solo existirá la sesión antigua en el llavero.
- **Compose:** variables exportadas desde `docker/empleado/.env` en la misma shell pueden sobreescribir `--env-file docker/admin/.env` y romper admin/GOG; usar `env -i … docker compose` o terminal limpia (documentado en `docker/README.md`).
- Regla endurecida en SOUL/skills/AGENTS raíz: solo cuenta de pruebas para gog; no citar otras direcciones en el chat.
- Tras `aes.KeyUnwrap integrity check failed` al guardar tokens: contenido de `~/.config/gogcli/keyring` movido a `keyring.bak-pre-reset-*`; `keyring/` recreado vacío. Misma **`GOG_KEYRING_PASSWORD`** en shell que en `docker/*/docker-compose` antes de `gog auth add`.
- OpenClaw **no** aceptaba `agents.defaults.rules` en esta versión → gateway en bucle (`Unrecognized key: "rules"`). Eliminado ese bloque en `docker/admin/openclaw.json` y `docker/empleado/openclaw.json`; política Gmail sigue en SOUL/AGENTS/skill. Stacks **admin** y **empleado** de nuevo **healthy**. Persisten avisos EACCES al escribir `/etc/openclaw/openclaw.json` (montaje); no impiden `gateway ready`.
- **`GOG_KEYRING_PASSWORD` y subprocess `gog`:** el gateway saneaba variables sensibles y no las pasaba al `exec` del skill; añadido `skills.entries.gog.env` en `docker/*/openclaw.json` con `GOG_KEYRING_*` / `GOG_ACCOUNT` vía `${…}` (skill empaquetada se llama **`gog`**, no `email-gmail`). Añadido **`XDG_CONFIG_HOME=/home/node/.config`** para que el llavero/`gogcli` resuelva bien en subprocesos.
- **Parche definitivo OpenClaw 2026.5.7:** la lógica de `applySkillConfigEnvOverrides` **no** pasaba `GOG_KEYRING_PASSWORD` a subprocesos si ya estaba en `process.env` (Docker) y **no** incluía claves de `skills.entries.*.env` en el allowlist de sanitización. Solución: **`entrypoint-gateway.sh`** escribe la contraseña en **`/tmp/openclaw-gog-keyring.pw`** al arrancar; **`gog-wrapper.sh`** reemplaza `/usr/local/bin/gog` y la lee antes de `exec gog.real`. Imágenes **admin/empleado** reconstruidas y contenedores recreados.
- Bot Telegram: mensaje de sistema (`telegram_context.py`) incorpora protocolo Gmail/gog alineado con `AGENTS.md`; comando `/correo` entra en modo chat con recordatorio borrador → confirmación en el mismo hilo (mismo gateway/credenciales que el asistente).
